services:
  - type: web
    name: livelink-server
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: LICENSE_KEYS
        sync: false
      - key: HWID_LOCK
        value: "0"
